<?php
namespace Coa\Models;

/**
 * Children classes are generated with (to be run from the app folder) :
 * doctrine orm:convert-mapping --namespace=Coa\\Models\\ --force --from-database annotation ./models --extend Coa\\Models\\BaseModel
 * doctrine orm:generate-entities models --filter=Coa\\\\Models
 */
class BaseModel
{
}