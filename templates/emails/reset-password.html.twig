{{ 'EMAIL_HELLO' | trans({'%userName%': user.username}) }}<br />
{{ 'EMAIL_RESET_PASSWORD_BODY' | trans({'%email%': user.email}) | raw }}<br />

<a href="{{ WEBSITE_ROOT }}/forgot/{{ token }}">
    {{ 'EMAIL_RESET_PASSWORD_LINK_TEXT' | trans }}
</a>

{{ include('emails/footer.html.twig') }}
